from .artifacts import (
    json_ready,
    load_json_file,
    load_model,
    path_to_json,
    release_torch_memory,
    save_model,
    write_json_file,
    write_setup_file,
)
from .backbones import (
    SUPPORTED_BACKBONES,
    configure_trainable_layers,
    count_parameters,
    describe_model,
    discover_model_blocks,
    finalize_training_config,
    format_parameter_count,
    forward_logits,
    get_logits,
    get_model_display_name,
    get_model_input_contract,
    get_model_native_input_size,
    list_supported_backbone_infos,
    list_supported_backbones,
    make_model,
    resolve_unfreeze_blocks,
)
from .config import (
    CLASS_NAMES,
    DATA_DIR,
    DEFAULT_ADV_EPSILON,
    DEFAULT_ADV_STEPS,
    DEFAULT_BATCH_SIZE,
    DEFAULT_COOLDOWN_EVERY_EPOCHS,
    DEFAULT_COOLDOWN_SECONDS,
    DEFAULT_DISTILLATION_ALPHA,
    DEFAULT_DISTILLATION_TEMPERATURE,
    DEFAULT_EPOCHS,
    DEFAULT_GPU_MAX_TEMP,
    DEFAULT_GPU_RESUME_TEMP,
    DEFAULT_GPU_TEMP_CHECK_SECONDS,
    DEFAULT_LR,
    DEFAULT_PATIENCE,
    DEFAULT_RESPLIT_RUNS,
    DEFAULT_TARGET_STEPS,
    DEVICE,
    IMAGE_SIZE,
    SCRIPT_DIR,
    TEST_RATIO,
    VALID_RATIO,
    TrainingConfig,
    TrainingHistoryEntry,
    build_training_config,
    config_to_json_dict,
    get_default_batch_size,
    get_learning_rate_for_split,
    load_setup_values,
    make_seed,
    merge_setup_and_cli_values,
    normalize_learning_rates_value,
    normalize_unfreeze_value,
    parse_learning_rates,
    resolve_effective_seed,
    set_seed,
)
from .contract import run_foreign_contract_check
from .cooldown import (
    CooldownConfig,
    CooldownEpochEndHandler,
    EpochEndHandler,
    build_cooldown_config,
)
from .data import (
    DatasetSourceSpec,
    LabeledDatasetView,
    PreparedTrainingDataset,
    get_dataset_classes,
    get_dataset_targets,
    load_dataset,
    load_dataset_source,
    make_data_loader,
    make_stratified_subset,
    split_train_dataset,
    to_device,
)
from .evaluation_runner import (
    DatasetEvalSpec,
    EvaluationPlan,
    EvaluationRunResult,
    ModelEvalSpec,
    evaluate_model_checkpoint,
    run_evaluation_plan,
)
from .figures import (
    evaluate_samples,
    generate_adversarial_example,
    generate_class_reversal_figure,
    generate_test_figure,
    generate_training_history_figure,
    is_notebook_environment,
    save_figure,
)
from .metrics import (
    DEFAULT_PRINT_METRICS,
    LOWER_IS_BETTER_METRICS,
    METRIC_CSV_FIELDS,
    METRIC_NAMES,
    ClassificationMetrics,
    DefaultValidationMetricComparator,
    ValidationComparison,
    evaluate_accuracy,
    evaluate_classification_metrics,
    evaluate_metrics,
    evaluate_validation_score,
)
from .paths import (
    AD_SAFE_RUNS_DIR,
    AD_SAFETY_DATASET_DIR,
    ARTEFACTS_DIR,
    CHALLENGE_DIR,
    DATASETS_DIR,
    PACKAGE_DIR,
    PATH_SEARCH_ROOTS,
    PROD_MODELS_DIR,
    SMOKE_MODELS_DIR,
    SWEEP_CONFIGS_DIR,
    ensure_artifact_dirs,
    path_for_json,
    resolve_existing_path,
    resolve_required_existing_path,
)
from .reporting import (
    MetricsCsvRow,
    MetricsMatrixRow,
    classification_metrics_from_mapping,
    flatten_metrics,
    format_csv_metric,
    format_metric,
    make_metrics_cell,
    metric_attr_name,
    metric_csv_name,
    metric_value,
    metrics_csv_fieldnames,
    metrics_from_flat_csv_row,
    print_metrics_matrix,
    sort_metrics_matrix_rows,
    write_metrics_csv_rows,
)
from .reversal import (
    ClassReversalContext,
    ClassReversalResult,
    ClassReversalStrategy,
    RandomRestartTargetClassStrategy,
)
from .training import (
    AdversarialAttackContext,
    AdversarialAttackStrategy,
    AdversarialPerturbationResult,
    BudgetedPgdStrategy,
    MinimalFlipPgdStrategy,
    compute_distillation_loss,
    compute_supervised_training_loss,
    extend_dataset_with_adversarial_samples,
    generate_adversarial_perturbation,
    precompute_teacher_logits,
    prepare_training_dataset,
    train_model,
    train_model_across_resplits,
    train_model_epoch,
)
from .training_runner import (
    JobSpec,
    PhaseSpec,
    RunPlan,
    TrainingPhaseResult,
    TrainingRunResult,
    phase_history_path,
    phase_json_path,
    phase_model_path,
    run_training_plan,
)
from .workflows import (
    describe_model_checkpoint,
    generate_single_model_artifacts,
)
